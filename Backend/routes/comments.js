import {Router} from "express";import {Comment,Post} from "../models.js";import guard from "../middleware/auth.js";const r=Router();r.get("/posts/:postId/comments",async(req,res)=>res.json({comments:await Comment.find({post:req.params.postId}).populate("author","name").sort("-createdAt")}));r.post("/posts/:postId/comments",guard,async(req,res)=>{if(!await Post.findById(req.params.postId))return res.status(404).json({message:"Post not found"});const c=await Comment.create({content:req.body.content,post:req.params.postId,author:req.user.id});res.status(201).json({comment:await c.populate("author","name")})});r.delete("/comments/:id",guard,async(req,res)=>{const c=await Comment.findById(req.params.id);if(!c)return res.status(404).json({message:"Comment not found"});if(c.author.toString()!==req.user.id)return res.status(403).json({message:"Only owner can delete"});await c.deleteOne();res.json({message:"Deleted"})});export default r;